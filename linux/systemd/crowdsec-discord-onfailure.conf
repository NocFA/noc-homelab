# Drop-in for /etc/systemd/system/crowdsec.service.d/
# Sends a Discord webhook alert if the CrowdSec agent enters failed state.
# Deployed via: see linux/scripts/install-systemd-failure-alerts.sh
[Unit]
OnFailure=notify-discord-failure@%n.service
