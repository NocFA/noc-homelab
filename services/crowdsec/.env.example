# Real values live in .env (gitignored / SOPS-encrypted on commit).

CROWDSEC_DISCORD_WEBHOOK=https://discord.com/api/webhooks/CHANGE_ME_ID/CHANGE_ME_TOKEN

# Log-triage webhook on noc-claw (Tailscale-only). Token must match the
# log-triage service's AUTH_TOKEN env.
LOG_TRIAGE_URL=http://noc-claw:8182/alert
LOG_TRIAGE_TOKEN=CHANGE_ME_32_HEX_CHARS
